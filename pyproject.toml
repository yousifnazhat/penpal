[project]
name = "penpal"
version = "0.1.0"
description = "Backend-first enumeration assistant for authorized pentesters."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
penpal = "penpal.cli:main"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

